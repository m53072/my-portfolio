Version: 0.1

phases:
    pre_build:
      commands:
        - npm install
        - npm test
    build:
      commands:
        - npm run webpack

artifacts:
  Files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
    - dist/bundle.js
